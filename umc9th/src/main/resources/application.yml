spring:
  application:
    name: "UMC"

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/UMC?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: jsh52454

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
